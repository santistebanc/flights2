import {
  ScrapedFlight,
  ScrapedBundle,
  ScrapedBookingOption,
} from "../types/scraper";

// TODO: Implement actual HTML parsing logic for each function

export function extractFlightsFromHtml(html: string): ScrapedFlight[] {
  // Parse HTML and extract flights
  return [];
}

export function extractBundlesFromHtml(html: string): ScrapedBundle[] {
  // Parse HTML and extract bundles
  return [];
}

export function extractBookingOptionsFromHtml(
  html: string
): ScrapedBookingOption[] {
  // Parse HTML and extract booking options
  return [];
}
